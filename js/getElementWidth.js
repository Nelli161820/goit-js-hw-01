function getElementWidth(content, padding, border) {
  // значение всех параметров:
  let content = '50px';
  let content = '60px';
  let content = '200px';
  let padding = '8px';
  let padding = '12px';
  let padding = '0px';
  let border = '4px';
  let border = '8.5px';
  let border = '0px';

  // преообразование рядка в число:
  const contentwidth = parseFl;

  // расчет общей ширины box-sizing
  const boxSizing = content + 2 * padding + 2 * border;

  console.log(boxSizing);
}
